console.log("Hello from the External File");

let gpa = 3.4;
    let deansList;
    if (gpa >= 3.5) {
        deansList = "Dean's List";
    }
    else if (gpa < 3.5) {
        deansList = "No Dean's List";
    }
    console.log (deansList);